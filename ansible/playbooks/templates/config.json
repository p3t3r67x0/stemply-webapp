{
  "SECRET_KEY": "{{ secret_key }}",
  "SECRET_SALT": "{{ secret_salt }}",
  "JWT_SECRET_KEY": "{{ jwt_secret_key }}",
  "MONGO_URI": "mongodb://zackig:{{ pwd_mongodb_user }}@127.0.0.1:27017/zackig",
  "MAIL_RECIPIENT": "{{ mail_recipient }}",
  "MAIL_SERVER": "{{ mail_server }}",
  "MAIL_PORT": "{{ mail_port }}",
  "MAIL_USERNAME": "{{ mail_username }}",
  "MAIL_PASSWORD": "{{ mail_password }}",
  "MAIL_USE_TLS": false,
  "MAIL_USE_SSL": true,
  "PROPAGATE_EXCEPTIONS": true,
  "BCRYPT_HANDLE_LONG_PASSWORDS": true,
  "APP_URL": "{{ domain_webapp }}",
  "RESET_PASSWORD_BODY": "Hello %NAME%,\n\nwith this mail we send you the link to reset your password for your ZackigOnline account.\n\n%LINK%\n\nBest regards\nZackigOnline Team",
  "CONFIRM_MAIL_BODY": "Hello %NAME%,\n\nwith this mail we send you the confirmation link for your ZackigOnline account.\n\n%LINK%\n\nBest regards\nZackigOnline Team"
}
